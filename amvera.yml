meta:
  environment: nodejs
  toolchain:
    name: npm
    version: "20"

build:
  skip: false
  additionalCommands: npm install

run:
  command: node index.js
  containerPort: 3000
  servicePort: 3000